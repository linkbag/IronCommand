{
  "name": "iron-command",
  "version": "0.1.0",
  "description": "Iron Command — A real-time strategy game inspired by classic RTS games. 15 factions, base building, resource gathering, and large-scale warfare.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test src/**/*.test.ts"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.1.0"
  }
}
